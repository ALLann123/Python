#!/usr/bin/python3

count=0
while (count<9):        #the loop executes so long as the expression turns true
	print("The count is:", count)
	count+=1                      #count=count+1

print("Goodbye!")             #executes after the loop expression turns to false
